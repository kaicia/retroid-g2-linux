# Codex Cloud trigger test

Temporary test file for verifying whether a GitHub pull request comment containing `@codex` can start a Codex Cloud task.

This file is test-only and should not be merged into `main`.
